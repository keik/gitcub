// @flow

import React from 'react'
import { storiesOf } from '@storybook/react'

import { Settings } from './Settings'

storiesOf('Repository/Settings', module).add('with default', () => <Settings />)
